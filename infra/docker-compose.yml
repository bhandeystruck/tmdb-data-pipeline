services:
  minio:
    image: minio/minio:latest
    container_name: tmdb-minio
    ports:
      - "9000:9000"     # S3 API
      - "9001:9001"     # Console UI
    env_file:
      - ../.env
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data

volumes:
  minio_data: